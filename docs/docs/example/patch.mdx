---
sidebar_position: 4
id: patch
title: Patch
---

import { Json, JsonResponse } from '@site/src/components/docs/patch';

## 1. JSON data

- If you want to send `json` data to the server, just add your javascript object in [json](../API/HttpOptions#json) property of `HttpOptions`.

<Json />
<br />
<br />

:::note
whenever users sends the json data to the server using [json](../API/HttpOptions#json) property of `HttpOptions`, it will set `content-type : application/json` header if `headers` object does not have `content-type` field.
:::

<br />

:::note
Here, res is just a javascript [Response](https://developer.mozilla.org/en-US/docs/Web/API/Response) object, so you can also access data as shown below.
:::

<br />
<JsonResponse />
