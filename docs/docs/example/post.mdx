---
sidebar_position: 2
id: post
title: Post
---

import { Json, FormData, JsonResponse } from '@site/src/components/docs/post';

## 1. JSON data

- If you want to send `json` data to the server, just add your javascript object in [data](../API/HttpOptions#data) property of `HttpOptions`.

<Json />
<br />
<br />

:::note
whenever users sends the json data to the server using [data](../API/HttpOptions#data) property of `HttpOptions`, it will set `content-type : application/json` header if headers object does not have `content-type` field.
:::

<br />

:::note
Here, res is just a javascript [Response](https://developer.mozilla.org/en-US/docs/Web/API/Response) object, so you can also access data as shown below.
:::

<br />
<JsonResponse />

## 2. FormData

-if you want to send [Formdata](https://developer.mozilla.org/en-US/docs/Web/API/FormData/Using_FormData_Objects) then just paas `FormData` object to [body](https://developer.mozilla.org/en-US/docs/Web/API/Request/body) property.

<FormData />
